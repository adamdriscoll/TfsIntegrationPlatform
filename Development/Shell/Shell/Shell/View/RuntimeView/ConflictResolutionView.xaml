<UserControl x:Class="Microsoft.TeamFoundation.Migration.Shell.ConflictResolutionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="300" Width="300">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="Rule Scope:"></Label>
            <TextBox Grid.Row="0" Grid.Column="1"> </TextBox>
        </Grid>
        <DockPanel Grid.Row="1">
            <Button  Height="23" HorizontalAlignment="Right" Margin="0,0,12,12" Name="ResolveButton" VerticalAlignment="Bottom" Width="75">Resolve</Button>
        </DockPanel>
    </Grid>
</UserControl>
